public enum Operator {
    // E' and U
    PLUS,
    MINUS,

    // T'
    MULTIPLY,
    DIVIDE,
    REMAINDER,

    // G and A
    PARENTHESES_OPENING,
    PARENTHESES_CLOSING,

    // G
    SQRT,
    LG,
    FACT,

    // A
    POW,
    LOG
}